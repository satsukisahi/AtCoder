and &
or |
xor ^
not ~
左シフト(大きくなる) << 
右シフト >>

bit全探索

d=個数

//0から2^d乗まで
for (int bit = 0; bit < (1<<d); ++bit) {

//各ビットを下から見ていく
     rep(j,d){

//jビット目が1なら処理を行う
        if(bit>>j&1){
            todo;
        }
    }
}