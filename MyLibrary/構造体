struct name
{
    ll x,y,cost;
};

sort用の比較関数　>なら降順
bool cmp(const name &a, const name &b)
{
    return a.y > b.y;
}
三項演算子を用いた方法 xの昇順で同じときyの昇順
bool cmp(const name &a, const name &b)
{
    return a.x == b.x ? a.y < b.y : a.x < b.x;
}