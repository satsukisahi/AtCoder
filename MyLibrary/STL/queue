キューは、最初に入れられたものが最初に取り出されます。
キューへのデータ追加は、キューの一番後ろです。


宣言
queue<pair<int, int>> q;


追加
q.push({0,0});


先頭要素
pair<int, int> p = q.front();

末尾
q.back();

先頭削除
q.pop();

あるかないか
q.empty()==0(なし)or1（ある）

要素数
q.size();