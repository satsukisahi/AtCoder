//隣接行列
int v;         //頂点数
int d[v][v]; //d[u][v]はuからvへコスト　存在しないとINF、d[i][i]=0

void warshallfloyd()
{
    for (int k = 0; k < v; k++)
        for (int i = 0; i < v; i++)
            for (int j = 0; j < v; j++)
                d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
}
